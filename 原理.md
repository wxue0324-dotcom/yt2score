# yt2score 是怎麼把一首歌變成樂譜的

這份文件寫給**沒學過程式的人**。看得懂國中理化和數學就夠了，不需要會看五線譜。

如果你只想知道一句話的答案：**電腦把聲音當成一長串數字，然後在這串數字裡找規律** ——
哪裡有音開始、多久跳一次、是什麼音高 —— 最後把找到的規律畫成譜。

---

## 先講大方向

一個人要把歌抄成譜，會做四件事：

1. 跟著點頭，抓到**速度**和拍子
2. 聽出這首是什麼**調**（簡譜的「1 = C」是什麼意思）
3. 在腦中把**樂器分開**聽，先聽主唱，再聽貝斯
4. 一個一個寫下每個音的**高低和長短**

程式做的是同樣四件事，只是它沒有耳朵也沒有音樂常識，全部要靠數學算出來。

```
YouTube 網址
  ↓ ①下載      把影片的聲音抓下來
  ↓ ②分析      找速度、拍號、調性
  ↓ ③分軌      把混在一起的樂器拆成四軌
  ↓ ④採譜      每一軌都變成一顆一顆的音符
  ↓ ⑤量化      把音符對齊到拍子的格線上
  ↓ ⑥清理      刪掉明顯是雜訊的音
  ↓ ⑦排版      畫成五線譜、簡譜，並合成試聽音檔
樂譜檔案
```

---

## 第 0 步：聲音在電腦裡長什麼樣子

聲音是空氣的振動。麥克風把振動的**強弱**每隔一小段時間量一次、記成一個數字。

這個程式每秒量 **22050 次**。所以一首 3 分鐘的歌，在程式眼中就是

> 約 400 萬個數字，排成一長串。

沒有「音符」，沒有「小節」，沒有「這是鋼琴那是鼓」。就只是 400 萬個上上下下的數字。

**接下來所有的步驟，都是在回答同一個問題：這 400 萬個數字裡，藏著哪些音樂資訊？**

可以想成看心電圖。心電圖也只是一條抖動的線，但醫生能從尖峰的位置讀出「每分鐘幾下」。
程式要做的事情類似，只是複雜很多。

---

## 第 ① 步：下載聲音（`download.py`）

用一個叫 **yt-dlp** 的工具，向 YouTube 要那支影片的音訊檔。

麻煩的是 YouTube 會防機器人。它會出一道**數學題**（官方叫 "n challenge"）：
給你一段程式碼，你得真的把它跑出答案，回傳正確才給你檔案。真正的瀏覽器天生會跑，
所以完全無感；一支下載工具就得另外裝一個能跑 JavaScript 的環境（本專案用 `deno`）來解題。

即使解對了，YouTube 有時仍然只放行低音質的版本。程式的做法是**階梯式退讓**：

> 先要最好的音質 → 被拒 → 要次好的 → 被拒 → …… → 最差的（49kbps）

只要有一級成功就繼續往下做，但會在畫面上明白警告你音質被降級了。
**這件事很重要**：音質差 → 分軌分不乾淨 → 採譜就跟著錯，錯誤是會一路累積下去的。

---

## 第 ② 步：找拍子和速度（`analyze.py`）—— 全程最難的一步

### 先找「音的開頭」

音樂裡每個音開始的瞬間，音量會突然衝上去。程式去找這串數字裡**突然變大的位置**，
每一個這種位置叫做一個**起音點（onset）**。

一首歌跑完會得到幾百個起音點，像這樣（每個 `|` 是一個音的開頭）：

```
時間 →
|   |  |   |   ||  |   |    |  |   |  |
```

看得出來有規律，但也很亂 —— 因為裡面混了主旋律、伴奏、鼓，還有一些根本是雜訊。

### 再從起音點找出穩定的拍

程式的策略是**猜答案再打分數**：

1. 猜一個速度，例如 100 BPM（每分鐘 100 拍）
2. 照這個速度畫一排等距的格線
3. 看這排格線跟剛才那些起音點**合不合**
4. 換一個速度再猜一次，猜十幾種，分數最高的當答案

打分數的方式要**兩邊互相檢查**：

| 檢查方向 | 問的問題 |
|---|---|
| 精確率 | 我畫的每一條格線上，真的有音嗎？ |
| 召回率 | 那些音，真的都踩在我的格線上嗎？ |

**兩個都要高才算好。** 只看其中一個一定會壞掉：如果只看「音有沒有踩在格線上」，
那我把格線畫得超級密（例如每 0.01 秒一條），每個音都一定被蓋到，分數滿分 —— 但這根本不是拍子。

### 八度歧義：這個問題數學上無解

假設一首歌真的是 100 BPM。現在把格線畫成 **200 BPM**（每一拍中間再插一條）：

```
真正的拍：  |     |     |     |
200 的格線：|  |  |  |  |  |  |     ← 原本的線一條不少，全都還在
```

**原本對的線全都還在**，所以「音有踩在線上」照樣成立。同樣地畫成 50 BPM（隔一條刪一條）也不會踩空。

換成人的說法：同一首歌，你可以**每小節拍一下手**、**每一拍拍一下**、或**每半拍拍一下**，
三種拍法全都跟得上音樂，沒有哪一種是「拍錯」。這就是**八度歧義**（速度差兩倍、四倍的模糊地帶）。

那人是怎麼決定的？靠感覺 —— 你會挑一個「腳自然會跟著踩」的速度。這是聽覺心理，不是數學。

程式只能模仿這個偏好：它額外給每個候選速度一個**合理度分數**，
**越接近每分鐘 120 拍（大約每秒兩下，人最自然的脈動）分數越高**，離越遠越低。
最後的總分 = 格線貼合度 × 合理度。

這個偏好故意設得很寬鬆，只是用來**打破平手**，不會蓋過證據 —— 所以真的很快或很慢的曲子還是贏得了。

### 它還是會錯，而且我們知道錯在哪

實測四首歌：

| 曲子 | 人耳聽出來 | 程式算出來 | |
|---|---|---|---|
| LOFI PIANO SAMPLE | 81 | 80.75 | ✅ |
| YOASOBI《勇者》 | 103 | 103.36 | ✅ |
| Merry Christmas Mr. Lawrence | 約 63 | 161.5 | ❌ 差很多 |

第三首錯得離譜，原因是它的鋼琴伴奏一直在跑細碎的音符，
**密的格線每一條都剛好踩得到音**，所以「畫太密」在計分上完全沒有被懲罰。
這是目前還沒解決的問題，寫在這裡是因為知道一個工具在哪裡會壞掉，比假裝它不會壞有用得多。

### 一小節幾拍（3/4 還是 4/4）

每小節的**第一拍通常最重**。所以程式把起音點的強度「每 3 個一數」和「每 4 個一數」都試一次，
看哪一種數法會讓「第一拍特別強」這件事更明顯。
因為流行音樂絕大多數是 4/4，程式預設 4/4，**3/4 要明顯贏才會被採用**。

---

## 第 ③ 步：找調性（`analyze.py`）

「調」決定簡譜上的 `1 = ?`。

一個八度裡只有 12 個音（Do、Do#、Re、Re#…）。程式把整首歌**每個音總共響了多久**統計成一張長條圖：

```
Do  ████████████
Do# ██
Re  ██████
Mi  █████████
Fa  ████
Sol ███████████
...
```

心理學家 Krumhansl 做過實驗，量出**大調和小調各自的典型長相**
（大調的 Do 最突出、Sol 次之，小調則是另一種形狀）。
程式把上面那張統計圖，跟 **24 種樣板**（12 個主音 × 大調/小調）一一比對，最像的那個就是答案。

像用指紋比對：不需要理解這首歌，只要找出「形狀最接近的那一個」。

**這件事程式做了兩次**，第二次才是重點：

- 第一次是拿**原始混音**去算，很吵 —— 鼓聲、殘響、噪音全都會混進統計裡
- 等到第 ④ 步採完譜、手上有一顆顆音符了，再用**音符**重算一次

第二次準確得多，因為那時候鼓聲已經被丟掉了，而且**音符有長度** ——
一個持續四拍的音，理應比一個一閃而過的裝飾音更能代表這首歌的調。
所以程式讓長的音權重更大，另外貝斯的權重再加成（低音最能定調）。

---

## 第 ④ 步：把樂器分開（`separate.py`）

用 **Demucs**，一個訓練好的**神經網路**。

神經網路是怎麼學會的？拿一大堆「已經分好軌的歌」（錄音室有完整分軌檔），
給它看混在一起的版本，要它猜四軌各長什麼樣，猜錯就把內部的參數調一點點 ——
這樣看過幾十萬次之後，它就抓到了規律。

它**並不理解音樂**。它學到的是「人聲的頻率大概落在哪、隨時間怎麼變化」這類統計上的規律。

分完會得到四個檔案：**人聲 / 鼓 / 貝斯 / 其他樂器**。

### 一個很容易踩的陷阱

Demucs **一定**會吐出四軌，就算你丟給它的是純演奏曲。
這時候「人聲」那一軌不是空的，裡面裝的是別的樂器漏過去的殘響 —— 一團模糊的聲音。

如果直接拿去採譜，就會**憑空生出一段根本不存在的主旋律**。

所以程式先量音量：**人聲軌的能量如果不到全曲的 8%，就判定這首沒有人唱，整軌跳過**，
並在結果裡告訴你「判定為純演奏曲」。

---

## 第 ⑤ 步：採譜（`transcribe.py`）

用 **basic-pitch**，另一個神經網路。

它的輸入是一小段聲音，輸出是「**這一瞬間，每一個音高正在響的可能性有多高**」——
可以想成一張表，橫軸是時間，縱軸是音高，格子越亮代表越可能有音。
然後把連續亮著的格子串起來，就變成一個音符：**起點、終點、音高、力度**。

### 泛音的麻煩

理化課學過：撥一根弦，它不只用整條振動，還會同時分成兩段、三段振動。
所以你彈一個 Do，空氣裡真的同時存在**高八度的 Do**、再上去的 **Sol**……只是比較小聲。
這叫**泛音**，是每種樂器音色不同的原因（少了泛音，鋼琴和小提琴聽起來會一模一樣）。

問題來了：泛音是真實存在的聲音，所以 basic-pitch 會誠實地把它報告成「有一個音」。
但譜上不該寫它 —— 沒有人彈那個音。

程式的判斷是，同時滿足**三個條件**才當成泛音刪掉：

1. 比另一個音**小聲很多**（不到六成）
2. 剛好在那個音的**上方 12、19、24 個半音**（正是泛音會出現的位置）
3. 它的**整段時間都被那個音包住**（泛音不可能活得比發出它的音還久）

為什麼要三個條件都成立？因為「真的有人唱高八度」在音樂裡太常見了，
只看音程就砍，會把大量真的音誤殺掉。

### 每種樂器用不同設定

| 音軌 | 只看這個頻率範圍 | 為什麼 |
|---|---|---|
| 人聲 | 75 – 1200 Hz | 人唱不到這範圍外，超出的必是別軌漏過來的 |
| 貝斯 | 30 – 400 Hz | 貝斯只在低音 |
| 其他樂器 | 55 – 3000 Hz | 要涵蓋鋼琴、吉他、弦樂 |

人聲那一軌還有一組看起來矛盾、其實很聰明的設定：
**開始一個音的門檻訂得很高（0.80），但延續一個音的門檻訂得很低（0.16）**。

意思是：「**要開始一個新的音，得拿出很強的證據；但一旦開始了，就寬鬆地一路跟著它。**」

如果延續的門檻也訂高，歌手的**顫音**（聲音自然的抖動）會讓音量忽上忽下，
一個長音就被切成七八個碎音，譜完全沒法看。

---

## 第 ⑥ 步：對齊格線（`quantize.py`）

到這裡，每個音的時間還是「第 12.3847 秒」這種數字。譜上不能這樣寫，
譜只有「四分音符、八分音符」這類固定長度。所以要把每個音**吸附到最近的格線上**。

### 為什麼只切到八分音符，不切十六分音符

因為**偵測本身的誤差，大約就有三十二分音符那麼大**。

格線切得比誤差還細，吸附的結果就不是在還原音樂，而是在**把雜訊當成音樂記錄下來** ——
譜面會爆出一堆亂七八糟的小音符，看起來很「精確」，其實全是噪音。
切到八分音符，出來的草稿反而乾淨得多，也好改得多。

**這是整個程式最重要的取捨：寧可粗一點但可信，不要細一點但全是假的。**

### 格線不是用「固定速度」畫的

真實的演奏會飄 —— 尤其現場演奏和有情緒起伏的曲子，速度一定會忽快忽慢。
所以格線是用**實際偵測到的每一拍位置**去內插的，而不是拿「一拍 0.6 秒」乘下去。
這樣曲子中途變慢，格線也會跟著變慢。

---

## 第 ⑦ 步：清理（`quantize.py`）

採出來的音符還是很髒，程式會做四件事：

**1. 合併重疊的同音**
同一個音高、時間上真的重疊 → 合成一個。
但**只有真的重疊才合併**。如果連「前後相鄰」也合併，
那貝斯刻意重複彈的 `Do Do Do Do` 就會被改寫成一個長長的 `Do——`，這是改錯歌，不是修雜訊。

**2. 修八度跳**
音高偵測器偶爾會整個猜錯八度。判斷方式：某個音如果比它左右鄰居的中位數高或低超過 9 個半音，
就把它上移或下移 12 個半音（一個八度），看哪個結果更貼近鄰居。
不修的話，譜上會冒出一堆掛在很遠加線上的怪音。

**3. 丟掉又短又小聲的音**
短音是正常的（快速樂句本來就短），小聲也正常（伴奏本來就輕）。
但**又短又小聲**的，幾乎都是分軌留下的碎屑。所以兩個條件同時成立才刪。

**4. 分左右手**
伴奏那一軌要寫成鋼琴的大譜表，得決定哪些音給右手、哪些給左手。
預設以中央 Do 為界，但如果這首歌整體偏低或偏高，分界會跟著音域移動。

---

## 第 ⑧ 步：排版與試聽（`score.py`、`audio.py`、`jianpu.py`）

**music21** 這個函式庫負責把音符組成一份正式的樂譜（加上譜號、拍號、調號、分小節），
**MuseScore** 負責把它畫成 PDF，並用內建的音源合成 MP3。

兩個看不見但很關鍵的細節：

**音長只允許幾種「乾淨」的值** —— 全音符、二分、四分、八分、以及它們的附點。
不然量化後那些 2.75 拍之類的長度，會被排版軟體寫成一大串連結線和雙附點音符，
理論上正確，實際上沒有人讀得下去。

**試聽 MP3 裡，主唱聲部被換成鋼琴音色。**
因為 MuseScore 合成人聲是一團沒有起音的「啊————」，你根本聽不出音高對不對。
換成鋼琴之後，每個音都有清楚的頭，**採錯的音一聽就露餡**。
用耳朵抓錯誤比用眼睛盯譜快太多了，這個檔案是拿來除錯的。

### 簡譜是怎麼來的

知道調（`1 = ?`）之後，把每個音高**減掉主音**，就知道它是第幾度 → 寫成 1~7。

| 符號 | 意思 |
|---|---|
| 數字上方加點 | 高一個八度（兩點就是高兩個八度） |
| 數字下方加點 | 低一個八度 |
| 數字下方畫線 | 長度減半（一條線 = 八分音符，兩條 = 十六分音符） |
| 右邊加橫線 `-` | 每一條多延長一拍 |
| 右邊加點 | 附點，長度變 1.5 倍 |
| `0` | 休止符 |

---

## 為什麼結果不會百分之百正確

四個環節都會出錯，而且**錯誤會一路累積**：

| 環節 | 會出什麼錯 | 後果 |
|---|---|---|
| 分軌 | 樂器沒分乾淨，有殘留 | 採到別的樂器的音 |
| 採譜 | 泛音被當成真音、和聲被漏聽 | 多音或少音 |
| 抓速度 | 抓成兩倍或一半 | **全部音符的長度整體變兩倍或一半** |
| 量化 | 對齊到八分音符 | 比八分音符更細的東西一律消失 |

所以這個程式的定位是**產生草稿，不是產生成品**。

輸出裡最有價值的其實是 `score.musicxml` —— 拿去用 MuseScore（免費）打開就能改。
先聽 `score.mp3` 找出哪幾小節不對，再回去改那幾小節，比從零開始抄快非常多。

---

## 名詞小抄

| 名詞 | 白話 |
|---|---|
| BPM | 每分鐘幾拍，數字越大越快 |
| 起音（onset） | 一個音開始的瞬間 |
| 泛音 | 彈一個音時，同時響起來的那些比較小聲的高音 |
| 分軌（stem） | 把混在一起的樂器拆開後，各自的音檔 |
| 量化 | 把不精準的演奏時間，對齊到譜上的格線 |
| 神經網路 | 靠看大量範例、反覆修正自己而學會做某件事的程式 |
| MusicXML | 樂譜的通用檔案格式，各家打譜軟體都能開 |
| MIDI | 只記錄「幾秒彈了哪個音」的檔案，不含聲音本身 |

---

## 程式檔案對照表

想去翻程式碼的話，每一步分別住在這裡：

| 步驟 | 檔案 |
|---|---|
| ① 下載 | `backend/pipeline/download.py` |
| ② 速度・拍號・調性 | `backend/pipeline/analyze.py` |
| ③ 分軌 | `backend/pipeline/separate.py` |
| ④ 採譜 | `backend/pipeline/transcribe.py` |
| ⑤⑥ 量化與清理 | `backend/pipeline/quantize.py` |
| ⑦ 五線譜 | `backend/pipeline/score.py` |
| ⑦ 簡譜 | `backend/pipeline/jianpu.py` |
| ⑦ 試聽音檔 | `backend/pipeline/audio.py` |
| 把以上串起來 | `backend/pipeline/run.py` |
